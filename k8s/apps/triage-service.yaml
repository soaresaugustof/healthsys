apiVersion: apps/v1
kind: Deployment
metadata:
  name: triage-service
  namespace: healthsys
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: triage-service
  template:
    metadata:
      labels:
        app: triage-service
    spec:
      containers:
        - name: triage-service
          image: ghcr.io/soaresaugustof/healthsys-triage-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: healthsys-config
            - secretRef:
                name: healthsys-secret
          resources:
            requests:
              memory: "200Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 120
            periodSeconds: 30
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: triage-service
  namespace: healthsys
spec:
  selector:
    app: triage-service
  ports:
    - port: 8083
      targetPort: 8083
